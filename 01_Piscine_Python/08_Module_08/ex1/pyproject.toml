[tool.poetry]
name = "loading"
version = "1.0.0"
description = ""

[tool.poetry.dependencies]
python = "^3.10"
pandas = ">=2.1.0"
numpy = ">=1.25.0"
requests = ">=2.31.0"
matplotlib = ">=3.7.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"